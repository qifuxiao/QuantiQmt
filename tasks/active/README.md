# Active Tasks

当前 active task：

- `TASK-004`：Implement Order persistence boundary, journal, snapshot, and outbox。
- `TASK-028`：Documentation authority, local environment, and team workflow cleanup。

TASK-014 已完成并进入 `tasks/completed/`。TASK-004 保持 active，状态和描述均未改变。TASK-028 是纯文档/治理任务，不授权修改 `src/`、`tests/`、`migrations/` 或业务契约。

TASK-015 已通过独立 Review、合并 PR #24，并根据人类明确授权移入 `tasks/completed/`。TASK-005 已解除 blocked，当前为 backlog/ready；TASK-005 尚未激活，不得开始实现。
