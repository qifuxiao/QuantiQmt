# Active Tasks

当前 active tasks：`TASK-005` 与 `TASK-028`。

TASK-005 已由人类授权激活。实现必须严格遵守任务中的 `allowed_paths`、`forbidden_paths`、`spec_refs` 和验证命令；完成后不得由实现 Agent 自行移动到 `completed/`。

TASK-028 由另一成员/会话执行 authority clean follow-up，仅修复获批的三份 Engineering Convention 文档，不修改 TASK-005 任务正文或实现。TASK-028 已恢复 active，须等待新 Head 的独立 Review 批准后才能移入 `completed/`。
